name: CI/CD Pipeline for To-Do List App

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

      - name: 🔐 Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🐳 Build & Push Backend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/git-todo-backend ./backend
          docker push ${{ secrets.DOCKER_USERNAME }}/git-todo-backend

      - name: 🐳 Build & Push Frontend Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/tgit-odo-frontend ./frontend
          docker push ${{ secrets.DOCKER_USERNAME }}/tgit-odo-frontend
